"use client";
import { useState } from "react";

const toggleFunc = () => {
  const [toggleDropdown, setToggleDropdown] = useState(false);
};

export { toggleFunc, toggleDropdown, setToggleDropdown };
